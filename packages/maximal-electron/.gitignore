out/
.vite/
node_modules/
dist/
dist-release/
staging/
*.log
.DS_Store
test-results/
playwright-report/
blob-report/
coverage/

.stryker-tmp/
reports/
demo/takes/

# Agent worktrees. Whole copies of this repository, created per task.
.claude/worktrees/

# Another agent tool writes its session database here.
.opencode/

# Storybook build output. A developer tool, never shipped.
storybook-static/

# pnpm is the package manager; npm lockfiles must not reappear
package-lock.json

# Research notes and their archive. Working material the build never reads.
research_log/
research_log.zip
