# CLAUDE.md

The instructions for this repository live in [AGENTS.md](./AGENTS.md).

This file is a pointer, not a copy. Two files with the same rules drift apart.

It is a real file rather than a symlink, because symlinks behave badly on
Windows checkouts.
