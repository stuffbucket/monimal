/**
 * The control vocabulary, re-exported.
 *
 * The primitives live in `controls/`, split by kind once there were too many
 * for one file to stay under the 300-line target in AGENTS.md. This file stays
 * so that every existing import keeps working.
 */
export * from './controls/index.js';
