node_modules/
dist/
# Astro per-project cache (regenerated on build)
.astro/
# build/ holds two flavors: compiled output (bun-build artifacts —
# ignore) and source-controlled installer templates under
# build/macos/ and build/homebrew/. The pattern below ignores
# everything in build/ except those source trees.
build/*
!build/macos/
!build/homebrew/
!build/windows/
SBOM.cdx.json
*.log
.DS_Store
.env
.env.local
.env.*.local

# bun
.bun/

# editor
.vscode/
.idea/
*.swp

# from upstream caozhiyuan/copilot-api
# local env
*.local
# aider
.aider*
# eslint cache
.eslintcache
.opencode/plans
.opencode/package-lock.json
*.vsix
.claude
contrib/slides/
# Stryker mutation testing
.stryker-tmp/
reports/
# bun --compile temp executables (live in cwd, ~60MB each)
.*.bun-build
dist-build/
dist-release/

*.dmg

# Build/bundle analysis report (generated by builds)
dist-report.html

# Generated UI embed manifest (regenerated by scripts/gen-ui-embed.ts)
src/generated/
