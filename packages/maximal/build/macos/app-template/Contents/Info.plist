<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleDevelopmentRegion</key>
  <string>en</string>

  <!-- Run the first-launch shim, not the binary directly. The shim
       handles install-vs-already-installed and exits; the launchd
       agent runs the actual proxy. -->
  <key>CFBundleExecutable</key>
  <string>first-launch</string>

  <key>CFBundleIdentifier</key>
  <string>co.stuffbucket.maximal</string>

  <key>CFBundleInfoDictionaryVersion</key>
  <string>6.0</string>

  <key>CFBundleName</key>
  <string>maximal</string>

  <key>CFBundleDisplayName</key>
  <string>maximal</string>

  <key>CFBundlePackageType</key>
  <string>APPL</string>

  <!-- Substituted by the installer workflow at build time. Keep the
       sentinel exact — installers.yml does a literal sed replace. -->
  <key>CFBundleShortVersionString</key>
  <string>__VERSION__</string>

  <key>CFBundleVersion</key>
  <string>__VERSION__</string>

  <key>CFBundleIconFile</key>
  <string>AppIcon</string>

  <!-- LSUIElement keeps us out of the Dock and the menu bar — the .app
       is a one-shot self-installer, not an interactive app. -->
  <key>LSUIElement</key>
  <true/>

  <key>LSMinimumSystemVersion</key>
  <string>14.0</string>

  <key>NSHighResolutionCapable</key>
  <true/>

  <key>NSHumanReadableCopyright</key>
  <string>MIT licensed. See LICENSE in the repository.</string>
</dict>
</plist>
