{
  "name": "monimal",
  "private": true,
  "type": "module",
  "description": "Monorepo spike: maximal, maximal-core and maximal-electron in one Bun workspace, driven by Turborepo.",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test --concurrency=1",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "check": "turbo run build typecheck && turbo run test --concurrency=1"
  },
  "devDependencies": {
    "turbo": "^2.10.9"
  },
  "engines": {
    "node": ">=22"
  }
}
